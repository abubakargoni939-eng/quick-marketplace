const P="qm_products_v3",C="qm_cart_v3";const get=(k,d)=>{try{return JSON.parse(localStorage.getItem(k))??d}catch{return d}};const set=(k,v)=>localStorage.setItem(k,JSON.stringify(v));const money=n=>"₦"+Number(n).toLocaleString("en-NG");
function render(){const ps=get(P,[]),c=get(C,[]),box=document.getElementById("cartItems");let total=0;if(!c.length){box.innerHTML="<p>Your cart is empty.</p>";checkoutLink.style.display="none";cartTotal.textContent="";return}checkoutLink.style.display="block";box.innerHTML=c.map(i=>{const p=ps.find(x=>x.id===i.productId);if(!p)return"";total+=p.price*i.qty;return`<div class="list-item"><span><b>${p.name}</b><br>${money(p.price)} × ${i.qty}</span><span><button class="secondary" data-minus="${p.id}">−</button> <button class="secondary" data-plus="${p.id}">+</button></span></div>`}).join("");cartTotal.textContent="Total: "+money(total);document.querySelectorAll("[data-plus]").forEach(b=>b.onclick=()=>change(b.dataset.plus,1));document.querySelectorAll("[data-minus]").forEach(b=>b.onclick=()=>change(b.dataset.minus,-1))}
function change(id,d){let c=get(C,[]),x=c.find(i=>i.productId===id);if(!x)return;x.qty+=d;if(x.qty<=0)c=c.filter(i=>i.productId!==id);set(C,c);render()}
document.addEventListener("DOMContentLoaded",render);