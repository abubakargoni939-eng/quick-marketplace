const P="qm_products_v3",C="qm_cart_v3",O="qm_orders_v3";const get=(k,d)=>{try{return JSON.parse(localStorage.getItem(k))??d}catch{return d}};const set=(k,v)=>localStorage.setItem(k,JSON.stringify(v));const money=n=>"₦"+Number(n).toLocaleString("en-NG");
document.addEventListener("DOMContentLoaded",()=>{const ps=get(P,[]),c=get(C,[]);let total=0;c.forEach(i=>{const p=ps.find(x=>x.id===i.productId);if(p)total+=p.price*i.qty});checkoutTotal.textContent="Order total: "+money(total);placeOrderBtn.onclick=()=>{const name=customerName.value.trim(),phone=customerPhone.value.trim(),city=deliveryCity.value.trim(),address=deliveryAddress.value.trim();if(!name||!phone||!city||!address){checkoutMessage.textContent="Please complete all delivery details.";checkoutMessage.className="msg danger";return}if(!c.length){checkoutMessage.textContent="Your cart is empty.";checkoutMessage.className="msg danger";return}const orders=get(O,[]),order={id:"ORD-"+Date.now(),createdAt:new Date().toISOString(),name,phone,city,address,total,items:c};orders.unshift(order);set(O,orders);set(C,[]);checkoutMessage.textContent="Order placed successfully. Order ID: "+order.id;checkoutMessage.className="msg success";placeOrderBtn.disabled=true}});
});